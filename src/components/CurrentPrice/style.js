import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerPrice: {
    width: "100%",
    height: "auto",
    alignItems: "center",
    marginBottom: 20,
  },
  CurrentPrice: {
    color: "#F50d41",
    fontSize: 32,
    fontWeight: "bold",
    paddingTop: 20,
  },
  TextPrice: {
    color: "#FFFFFF",
    fontSize: 16,
    fontWeight: "bold",
  },
});

export default styles;
